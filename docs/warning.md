# 注意事项

1. **API Key 安全** - 不要在前端代码中暴露 API Key
2. **实时连接** - 使用 SSE 建立长连接，自动重连机制
3. **用户标识** - 使用 `oId` 作为用户唯一标识
4. **消息推送** - 只能推送给在线用户
5. **VIP管理** - 同一用户同一VIP类型只能有一条记录
6. **时间格式** - 统一使用 `2006-01-02 15:04:05` 格式

---

## 技术支持

如有问题，请联系技术支持或查看完整文档。